enum Environment { staging, production }

enum LoadState {
  none,
  loading,
  success,
  failure,
}

enum FilterTime {
  latest,
  oldest,
}

enum FilterStatus {
  latest,
  oldest,
}

enum SelectAddressType {
  province,
  district,
  ward,
}
